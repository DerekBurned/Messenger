<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Messenger"
        tools:targetApi="31">

        <!--
        Вот единственная и правильная запись для вашей MainActivity.
        Она указывает правильный путь, делает ее стартовой (LAUNCHER)
        и разрешает системе ее запускать (exported="true").
        -->
        <activity
            android:name=".presentation.screens.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Messenger">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        Запись для ChatActivity оставляем на будущее.
        Она не стартовая, поэтому exported="false".
        -->
        <activity
            android:name=".presentation.screens.ChatActivity"
            android:exported="false"
            android:label="@string/title_activity_chat"
            android:theme="@style/Theme.Messenger" />

    </application>

</manifest>
